{
  "name": "flooring-calculator-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for the Flooring Calculator WordPress plugin",
  "scripts": {
    "build:css": "tailwindcss -i ./src/index.css -o ./assets/flooring-calculator.css --minify",
    "build:js": "node scripts/build.js",
    "build": "npm run build:css && npm run build:js"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0"
  }
}
